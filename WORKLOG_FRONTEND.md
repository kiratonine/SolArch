# SolArch — Worklog: Marketplace Frontend

**Роль:** участник 2 — Marketplace Frontend
**Ветка:** `feat/marketplace-frontend`
**Владение кодом:** `apps/web` (и только он)
**Source of truth по контрактам:** `docs/API.md`, `docs/INTEGRATION.md`, `docs/SPEC.md`, `docs/DECISIONS.md`

> Этот файл — память между сессиями. Изменяемое состояние живёт здесь,
> неизменное сжатое описание проекта — в `CLAUDE.md` (загружается автоматически).

---

## 0. Как пользоваться файлом

Начало сессии:
```text
1. CLAUDE.md загружается автоматически — там сжатый контекст проекта.
2. Прочитать этот файл: «Текущее состояние» + первый незакрытый этап.
3. docs/*.md читать точечно и только если этап того требует.
4. НЕ читать docs/context/* — устаревший черновик (старое имя продукта и расширение).
```

Конец сессии:
```text
1. Обновить статусы этапов.
2. Добавить запись в «Журнал сессий».
3. Записать новые решения / открытые вопросы.
```

---

## 1. Что мы строим

Публичный Marketplace + Creator Dashboard.

Два пользовательских пути:
```text
Visitor:  Discover → Inspect → Download .slr   (без регистрации)
Creator:  Login → Create Archive → Upload → Configure → Publish → Analytics
```

Frontend **не** отвечает за: payment verification, entitlement, device license, `.slr` crypto,
Solana-транзакции, приватные ключи, подсчёт метрик. Всё это — backend/viewer.
Оплата происходит внутри Viewer, не в браузере.

Ключевые продуктовые константы (менять нельзя):
```text
USDC only · platform fee 5% · creator 95% · price immutable after create
max_devices = 1 · network fees paid by SolArch · non-custodial payout wallet
public metrics = views / downloads / paid_unlocks
supported content = PDF, PNG, JPG/JPEG, WebP, DOCX, XLSX
```

### Стек

```text
React + TypeScript + Vite      SPA, без Next.js — SEO проекту не нужен
TanStack Query                 кеш, staleTime, инвалидация, retry
TanStack Router                типобезопасные роуты и search params (?sort=)
Tailwind CSS
shadcn/ui на Base UI           Base UI — дефолт shadcn с релиза 2026-07
MSW                            мок backend на уровне сети; те же хендлеры в тестах
zod                            схемы DTO = контракт + рантайм-проверка ответов
Структура                      apps/web как npm workspace (INTEGRATION.md §2)
```

Одобрены к использованию, но ещё не подключены (ставим по месту применения):
`react-hook-form` + `zod` резолвер — на S7 (форма создания архива);
`zustand` — только если появится клиентское состояние, которое не покрывает TanStack Query.

Проверено по актуальной документации: `npx shadcn@latest init -t vite` даёт шаблон Vite;
CLI v4 поддерживает флаг `--base` для выбора примитивов, Base UI идёт по умолчанию.
Точные команды фиксируем на S0 после реального запуска CLI.

---

## 2. Дорожная карта (этапы ≈ сессии)

Статусы: `todo` / `in-progress` / `done`

| # | Этап | Статус | Комментарий |
|---|------|--------|-------------|
| S0 | Скелет `apps/web` | **done** | Vite 8, React 19, TS 6, Tailwind v4, shadcn/Base UI, TanStack Query+Router, MSW, Vitest, oxlint |
| S1 | Typed API client + mock-слой (`lib/api/*`) по `docs/API.md` | **done** | zod-схемы как контракт, MSW с состоянием, 35 тестов |
| S2 | Дизайн-язык: тема, layout, базовые компоненты, i18n | **done** | Тема «запечатано на виду», en/ru, тёмная тема, 61 тест |
| S3 | Public: Landing / Catalog + сортировки + состояния | **done** | Сортировки, поиск и страница в URL; три шага механики; 90 тестов |
| S4 | Public: Archive Page + file listing + metrics + CTA Download `.slr` | **done** | Опись файлов, условия лицензии, обложка, объяснение оплаты, прелоад по наведению; 104 теста |
| S5 | Creator auth (wallet challenge/verify) + guard роутов + сессия | **done** | Wallet Standard вместо сниффинга расширений, вход в два шага, guard в `beforeLoad`; 129 тестов |
| S6 | Creator Dashboard: My Archives | **done** | Оба статуса порознь, доля автора, опрос сборки; 144 теста |
| S7 | Create Archive: форма + экономика 95/5 + immutable price warning + payout wallet | todo | Валидация Solana-адреса |
| S8 | Upload UX: ZIP/файлы, progress, processing, ready/failed | todo | Никакой крипты в браузере |
| S9 | Publish / Unpublish + preview публичной карточки | todo | |
| S10 | Analytics: периоды 7d/30d/all, конверсии, revenue | todo | Цифры только с backend |
| S11 | Тесты: unit/component + integration на mock API | todo | Список в `docs/TESTING.md` §5 |
| S12 | Интеграция с реальным backend + E2E (12 шагов из роли §14) | todo | Зависит от ветки backend |
| S13 | README, команды запуска, проверка отсутствия секретов | todo | Definition of Done |

---

## 3. Карта страниц

Клиентский роутинг (SPA), не файловый:

```text
/                         Landing + каталог (+ ?sort=)
/archives/:slug           Публичная страница архива
/download                 Скачать SolArch Viewer
/how-it-works             Как это работает — планируется: туда уходят объяснения
                          механики, снятые с главной и из подвала (F41, F55)
/login                    Creator auth (wallet signature)
/dashboard                My Archives
/dashboard/new            Create Archive (метаданные + цена + payout wallet + policy)
/dashboard/:id            Archive Details: upload, статусы, publish/unpublish, public link
/dashboard/:id/analytics  Creator analytics
```

Сортировки каталога (значения передаём в backend как есть):
```text
popular_week | popular_month | most_downloaded | price_asc | price_desc
```

---

## 4. Контракты, от которых зависим

Эндпоинты, которые использует фронт (из `docs/API.md`):

```text
POST /v1/auth/wallet/challenge
POST /v1/auth/wallet/verify
GET  /v1/me
POST /v1/auth/logout

POST  /v1/archives
GET   /v1/archives/:archiveId
PATCH /v1/archives/:archiveId
POST  /v1/archives/:archiveId/publish
POST  /v1/archives/:archiveId/unpublish
GET   /v1/archives/:archiveId/download
GET   /v1/archives/:archiveId/analytics?period=7d|30d|all

POST /v1/uploads/init
PUT  <upload transport>
POST /v1/uploads/:uploadId/complete
POST /v1/uploads/:uploadId/cancel

GET /v1/marketplace/archives?sort=&search=&category=&page=
GET /v1/marketplace/archives/:slug
GET /v1/marketplace/archives/:slug/files
GET /v1/marketplace/archives/:slug/download
```

Общие статусы (нельзя выдумывать свои строки):
```text
technical:   draft | uploading | processing | ready | failed
marketplace: draft | published | unpublished | blocked
```

Деньги: decimal string (`"10.00"`), не float. Превью 95/5 можно считать до create,
после create показываем `economics` из ответа backend.

Публичный file listing — только эти поля:
```text
display_path · display_name · extension · mime_type · size_bytes
```

---

## 5. Текущее состояние

```text
Remote:      https://github.com/kiratonine/SolArch — main и feat/marketplace-frontend запушены
apps/web:    скелет + API-слой + моки + дизайн-язык и i18n + каталог + страница архива
             + вход автора по кошельку, сессия и закрытый кабинет
             + список своих архивов в кабинете
Backend:     недоступен → работаем на MSW
Этап:        S0–S6 done → следующий S7
```

Проверено на S6: `tsc -b`, `oxlint` (ноль предупреждений), `vitest` (144/144), `vite build`.
Демо-кошелька в production-сборке по-прежнему нет — проверено поиском по `dist`.

Живьём пройден весь кабинет: вход по кошельку, список из шести архивов, все пять
состояний карточки, диалог выхода (кнопкой, кликом мимо, Escape; фокус возвращается
на кнопку), 1280px и 390px, светлая и тёмная тема, обе локали. Горизонтальной
прокрутки нет ни на одной ширине — проверено обходом дерева, а не глазом.
Переход `processing → ready` увиден на экране: карточка обновилась сама, и опрос
после этого прекратился (за 14 секунд ни одного запроса).

В консоли на публичных страницах остаётся та же одна строка: `401` на `GET /v1/me`
у гостя (Q12). Новых ошибок нет.

В консоли на публичных страницах остаётся одна строка: `401` на `GET /v1/me`.
Это не ошибка приложения, а ответ мока гостю; чем ответит настоящий backend — Q12.

Организационный вопрос (открыт): по `INTEGRATION.md` §12 первым шагом идёт
«merge common docs and monorepo skeleton» — надо договориться с командой,
что `main` становится общей базой, от которой ответвляются две другие ветки.
Иначе три ветки разойдутся на разных версиях документации и корневого конфига.

---

## 6. Принятые решения (frontend-local)

| # | Решение | Дата | Причина |
|---|---------|------|---------|
| F1 | React + Vite SPA вместо Next.js | 2026-09-07 | SEO не требуется, SSR — оверкил; бэкенд отдельный на NestJS |
| F2 | TanStack Query как слой данных | 2026-09-07 | Кеш, staleTime, инвалидация, ретраи из коробки |
| F3 | Tailwind CSS | 2026-09-07 | Скорость + совместимость с shadcn |
| F4 | shadcn/ui на примитивах Base UI | 2026-09-07 | Base UI качественнее Radix и с 2026-07 — дефолт shadcn |
| F5 | Тему меняем заменой токенов/шаблона shadcn | 2026-09-07 | Не переписывать компоненты при смене визуала |
| F6 | `docs/context/*` объявлен устаревшим | 2026-09-07 | Старое имя продукта, старое расширение, старая бизнес-логика |
| F7 | TanStack Router | 2026-09-07 | Один вендор с Query; типобезопасные search params — сортировка каталога живёт в URL |
| F8 | MSW для мока backend | 2026-09-07 | Клиент сразу пишется как боевой; на S12 моки просто выключаются; хендлеры переиспользуются в тестах и E2E |
| F9 | `apps/web` как npm workspace | 2026-09-07 | `INTEGRATION.md` §2 закрепляет `apps/web` за нашей веткой; готово к слиянию трёх веток |
| F10 | DTO в snake_case, как на проводе | 2026-09-07 | Файл сверяется с `docs/API.md` построчно; нет тихих ошибок маппинга при интеграции |
| F11 | zod-схемы как источник типов и рантайм-проверки ответов | 2026-09-07 | Бэкенда нет; расхождение с контрактом падает на границе сети с внятным сообщением, а не как `undefined` в компоненте |
| F12 | Расчёт денег на `bigint` в base units | 2026-09-07 | `PAYMENTS.md` §5 запрещает float; комиссия вниз, автору остаток — `creator + platform === price` всегда |
| F13 | Мок с состоянием в памяти, а не статика | 2026-09-07 | Иначе невозможно проверить переходы draft → uploading → processing → ready → published |
| F14 | oxlint вместо ESLint | 2026-09-07 | Дефолт `create-vite@9`; быстрее и меньше конфигурации |
| F15 | i18n на своём типизированном словаре, без библиотеки | 2026-09-07 | `ru` объявлен как `typeof en` — забытый ключ падает на `tsc`, а не всплывает пустотой в UI; плюрализация и форматы через `Intl`; ноль новых зависимостей |
| F16 | Английский по умолчанию, русский по `navigator` | 2026-09-07 | Оплата в USDC, аудитория международная; явный выбор хранится в localStorage и важнее языка браузера |
| F17 | Дизайн-язык «запечатано на виду»; янтарь только за деньги | 2026-09-07 | Один акцент с одним смыслом читается как система. Два токена: `--seal` для заливок, `--seal-ink` для текста (контраст AA) |
| F18 | Unbounded (заголовки) + Golos Text (интерфейс) + IBM Plex Mono (машинные строки) | 2026-09-07 | Archivo отпал — в нём нет кириллицы. Выбранные три семейства несут и латиницу, и кириллицу: `en` и `ru` живут в одной типографике без подмен |
| F19 | Ни одной тени; глубина — разница бумага/поверхность плюс волосяные линии | 2026-09-07 | Одинаковые карточки с одинаковой мягкой тенью — самый заметный признак шаблонной вёрстки |
| F20 | Тема применяется скриптом в `index.html` до первой отрисовки | 2026-09-07 | Иначе тёмная тема моргает белым на каждой загрузке. Ключ намеренно продублирован в `lib/theme.ts` — менять в двух местах |
| F21 | Одна мера ширины 54rem на шапку, контент и подвал | 2026-09-07 | Общая левая и правая кромка. Более широкая мера появится, когда её реально потребует кабинет автора (S6), а не «про запас» |
| F22 | Состояние каталога (`sort`, `q`, `page`) живёт в search params, не в компоненте | 2026-09-07 | Любую выдачу можно переслать ссылкой, положить в закладки и вернуть кнопкой «назад». Разбор один на всё приложение — `lib/catalog-search.ts` |
| F23 | Мусор в параметрах гасится в умолчание, а не бросает исключение | 2026-09-07 | Адрес каталога публичный: его правят руками и обрезают при пересылке. `?sort=что-нибудь` обязан дать каталог, а не белый экран. Каждое поле схемы завёрнуто в `.catch(undefined)` |
| F24 | Значения по умолчанию в адресе не хранятся | 2026-09-07 | `/` вместо `/?sort=popular_week&q=&page=1`. Все ссылки каталога собираются одной функцией `catalogLinkSearch` |
| F25 | Сортировки — ссылки, а не выпадающий список | 2026-09-07 | У каждого порядка выдачи свой адрес; работает средняя кнопка мыши и открытие в новой вкладке. Пять коротких подписей в меру ширины помещаются |
| F26 | `aria-current` на чипе сортировки ставим сами | 2026-09-07 | Роутер считает ссылку активной по точному совпадению адреса; на второй странице совпадения нет, а подсветка нужна. Значение то же (`page`), поэтому расхождения не возникает |
| F27 | Ввод в поиск не сбрасывает прокрутку (`resetScroll: false`) | 2026-09-07 | Поиск пишет в адрес каждые 300 мс, и при `scrollRestoration` каждая запись утаскивала страницу к шапке. Найдено на живой странице, не тестом |
| F28 | `keepPreviousData` в каталоге | 2026-09-07 | Смена сортировки или страницы не схлопывает список в скелет: прошлая выдача остаётся и приглушается. Тесты обязаны ждать новую выдачу, а не первую попавшуюся |
| F29 | 4xx не повторяются | 2026-09-07 | 404 на снятый с публикации архив повтором не исправить — он только задерживал показ страницы. Повторяем сбои сети и 5xx |
| F30 | `PER_PAGE` в моке уменьшен до 6, фикстур стало девять | 2026-09-07 | На пяти архивах и странице в 12 постраничность не появлялась вовсе и осталась бы непроверенной |
| F31 | Опись файлов группируется по `display_path`, порядок backend не меняется | 2026-09-07 | Структуру несёт только путь — отдельного поля папки в контракте нет. Сортировать по алфавиту значило бы переставлять содержание книги: порядок файлов задаёт автор |
| F32 | `extension` и `mime_type` в описи не рисуем | 2026-09-07 | Имя файла уже кончается расширением. Повторить его в каждой из сорока строк — шум, а не сведения; поля остаются в контракте и в типах |
| F33 | Условия лицензии — прозаический перевод `license_policy`, а не вывод полей | 2026-09-07 | Гость платит уже после скачивания, поэтому «что даёт оплата» обязан прочесть до него. Выключенный водяной знак строки не даёт вовсе: говорить «его нет» не о чем |
| F34 | Обложка живёт в жёстком кадре 1200×630 с `object-cover` | 2026-09-07 | Пропорции обложек контракт не оговаривает (Q3). Без фиксированного кадра каждая обложка двигала бы весь текст под собой при загрузке. Битая ссылка убирает блок целиком |
| F35 | Прелоад страницы архива — `prefetchQuery` в loader, а не `ensureQueryData` | 2026-09-07 | `ensureQueryData` бросил бы 404 снятого архива в обработчик ошибок роутера, и вместо «архив недоступен» человек увидел бы аварийный экран. `prefetchQuery` не бросает и не блокирует переход |
| F36 | Логотип собран в один локап `Logo`, знак приподнят на 6% своей высоты | 2026-09-07 | Шапка и подвал собирали лого каждая по-своему и рано или поздно разошлись бы. `items-center` равняет боксы, а глаз равняет прописные, которые стоят выше центра бокса: без поправки знак проседает. Поправка долей, а не пикселями — работает на любом кегле |
| F37 | Силуэт знака стал целым прямоугольником, печать ушла внутрь контура | 2026-09-07 | Срез у самого силуэта на 16–18 пикселях читался как криво обрезанный прямоугольник, а не как печать. Целый контур вдобавок точнее выражает тему: контейнер виден полностью |
| F38 | У каждой фикстуры свой автор; мок знает владельца архива | 2026-09-07 | Каталог с одним автором на всех карточках не показывает ни длинного имени, ни того, что автор вообще меняется. Владелец (`owner_id`, вне контракта) нужен, чтобы `GET /archives` отдавал кабинет автора, а не весь каталог — иначе S6 строился бы на заведомо неверных данных |
| F39 | Поиск в моке ищет и по имени автора | 2026-09-07 | Посетитель чаще помнит, чей это архив, чем как он назывался. Поиск, который на «Studio Kirn» отвечает «ничего нет», выглядит сломанным. Состав полей — допущение (Q11), плейсхолдер поля переписан под него |
| F40 | Счётчик архивов из шапки каталога убран вместе с пропом `aside` | 2026-09-07 | Число ничего не решает: посетитель видит карточки и постраничность, а «9 архивов» рядом с заголовком читается как показатель, за которым ничего не стоит. Проп `aside` больше некому передавать, и держать его пустым — заготовка «на будущее» |
| F41 | Блок «три шага механики» с главной убран целиком | 2026-09-07 | Решение заказчика. Треть блока была адресована не покупателю, а автору (доля 95%), и та же мысль уже стоит в подвале. Продукт объясняют подзаголовок каталога («забрать без регистрации, оплата позже в Viewer») и подвал; роль §3.1 этим удовлетворена, экран до списка стал на 200 пикселей короче |

| F42 | Кошельки берём из реестра Wallet Standard (`@wallet-standard/app`), а не из `window.phantom` | 2026-09-07 | Сниффинг `window` — переписанный руками устаревший протокол, который стандарт и заменил. В реестре расширения объявляют себя сами: приложение не знает их имён, а имя и иконку каждого кошелька отдаёт он сам — своих логотипов не рисуем и не устареваем |
| F43 | `@solana/wallet-adapter` отвергнут | 2026-09-07 | Тянет peer `@solana/web3.js` (RPC, Connection, транзакции) и свою модалку с чужой визуальной системой. Транзакции фронт не строит никогда: оплата идёт в Viewer, выплаты non-custodial. Нужны ровно две операции — `standard:connect` и `solana:signMessage`, обе есть в стандарте напрямую |
| F44 | Вход в два шага: подключение, затем подпись — между ними сообщение показано целиком | 2026-09-07 | Один клик короче, но приучает подписывать вслепую, а на этой привычке и работает фишинг кошельков. Плита с текстом набрана моно: это машинная строка, и она — главный элемент экрана |
| F45 | Guard кабинета стоит в `beforeLoad`, а не в компоненте | 2026-09-07 | Пока сессия не подтверждена, перехода не происходит вовсе: приватная страница не успевает мигнуть. Адрес, куда человек шёл, уезжает в `redirect` и возвращает его туда же после входа |
| F46 | Адрес возврата пропускается через `safeRedirect` | 2026-09-07 | `/login?redirect=https://evil.example` присылают в письме затем, чтобы вход на настоящем сайте закончился на поддельном. Пропускаем только путь внутри приложения: `//host` и `/\\host` браузер выпрямляет в чужой адрес |
| F47 | 401 обрабатывается один раз в `QueryCache.onError`, наружу уводит `router.invalidate()` | 2026-09-07 | Просроченная сессия приходит не событием, а ответом на первый же запрос кабинета. Обработчик, разложенный по запросам, рано или поздно был бы забыт в одном из них. Клиент запросов не знает про роутер — связка задаётся один раз в `main.tsx` |
| F48 | Демо-кошелёк живёт в `mocks/` и регистрируется через тот же реестр | 2026-09-07 | Иначе вход невозможно пройти в браузере без расширения. В приложении при этом ноль веток «если демо», а в production-сборке его нет: модуль импортируется динамически и только под `import.meta.env.DEV` |
| F49 | `SectionHeading` вынесен в общий компонент | 2026-09-07 | Базовый слой стилей назначает `font-display` всем h1–h3, поэтому каждый новый раздел по умолчанию получает Unbounded вопреки правилу «Unbounded только h1 и логотип». Снимать это в каждом файле руками — значит однажды забыть |
| F50 | В шапке на узком экране ссылка «Каталог» скрыта | 2026-09-07 | У вошедшего автора в строке пять органов управления, и на 390px шапка уезжала за правый край вместе со всей страницей. Ссылку в каталог несёт логотип — он и так ведёт на «/» |
| F51 | На каркасе кабинета нет пустого состояния «архивов пока нет» | 2026-09-07 | Это утверждение о данных, которых страница ещё не запрашивает: список приходит на S6, а у автора в моке архивы как раз есть. Вместо выдумки показан факт — каким кошельком человек вошёл, полным адресом |
| F52 | Ссылки шапки набраны основным цветом; где человек находится, показывает волосяное подчёркивание | 2026-09-07 | Решение заказчика: приглушённая навигация читалась второстепенной, хотя другой на сайте нет. Активную страницу нельзя отметить цветом — янтарь закреплён за деньгами (F17), поэтому её несёт подчёркивание |
| F53 | Адрес кошелька в шапке убран в плашку с рамкой и поставлен в ряд с переключателями языка и темы | 2026-09-07 | Решение заказчика: моноширинный адрес среди ссылок читался сломанной ссылкой. Адрес — не пункт навигации, а сведение о том, кем ты вошёл, поэтому получил форму плашки, а не ссылки |
| F54 | Раздел описи назван «Содержимое», пояснение под ним убрано | 2026-09-07 | Решение заказчика. Ключ `archive.files.lead` удалён из обеих локалей — мёртвых ключей не держим. Мысль «имена открыты, содержимое запечатано» остаётся в подвале и в разделе «Что даёт оплата» |
| F55 | Подвал сведён к логотипу и копирайту; продуктовые объяснения уезжают на отдельную страницу | 2026-09-07 | Решение заказчика: текста стало слишком много и он повторялся на каждой странице. Объяснение механики — это одна страница «как это работает», а не приписка в подвале под каждым экраном. Ключи `footer.mechanic` и `footer.fee` удалены из обеих локалей |

| F56 | Карточка кабинета — отдельный компонент, а не режим каталожной | 2026-09-07 | Каталожная построена на `MarketplaceArchiveListItem` и ведёт по slug, которого у черновика нет вовсе. Общее у них — корешок, манифест и цена, и это уже отдельные компоненты. Один компонент на две роли разошёлся бы пропсами-переключателями на каждое отличие |
| F57 | Оба статуса показываем порознь и всегда | 2026-09-07 | `technical` описывает файл, `marketplace` — витрину, и расходятся они в обе стороны: готовый архив снят с продажи, опубликованный — со сломанной пересборкой. Пара «Ready + Published» не шум, а два разных факта; автору важно видеть, какой из них изменился |
| F58 | Корешок читает два статуса по срочности | 2026-09-07 | Полоса одна, статусов два. Порядок: сломанная сборка → незаконченная → витрина. Опубликованный получает чернильный корешок, как в каталоге: он там же, где его видит покупатель. Правило лежит в `tone.ts` рядом со шкалой, а не спрятано в компоненте |
| F59 | Выручки за период в кабинете нет; показываем долю автора из `economics` | 2026-09-07 | Агрегата «сколько заработано» в контракте не существует, а перемножить `paid_unlocks` на долю автора — значит выдумать число, за которое никто не отвечает: возвраты, частичные периоды, разные комиссии у разных архивов. Показываем присланное: цену и долю автора с каждого открытия. Настоящая выручка придёт из `analytics` на S10 (Q14) |
| F60 | Манифест одинаков у всех карточек, даже когда весь в нулях | 2026-09-07 | Одинаковые колонки позволяют сравнивать архивы взглядом вниз по списку. «0 просмотров» у черновика — такой же факт, как 18 420 у опубликованного, а прятать нули значило бы менять геометрию карточки от строки к строке |
| F61 | Порядок списка задаёт фронт: новые сверху | 2026-09-07 | Параметра сортировки у `GET /archives` в контракте нет, список приходит целиком и он короткий. Автор возвращается к тому, над чем работал последним. Сравнение через `Date`, а не построчно: часовой пояс и точность `created_at` контракт не оговаривает |
| F62 | Список сам опрашивает backend, пока хоть один архив собирается | 2026-09-07 | `uploading` и `processing` кончаются на стороне backend. Без опроса кабинет показывал бы «обрабатывается» до ручной перезагрузки. Опрос прекращается сам, когда переходных архивов не осталось — тот же приём, что уже был у запроса одного архива |
| F63 | Выход спрашивает подтверждение | 2026-09-07 | Решение заказчика. Кнопка стоит вплотную к ссылкам шапки, и промах выбрасывал автора из кабинета мгновенно. Данных это не теряет, но вернуться можно только через кошелёк и новую подпись — цена случайного нажатия выше, чем у любой другой кнопки на сайте. |
| F64 | Диалог выхода отпускает по Escape и по клику мимо | 2026-09-07 | Правка заказчика по ходу сессии. Сперва был взят `AlertDialog`, который специально не закрывается кликом вне себя, — но вопрос «выйти?» не то, ради чего стоит запирать экран. Любой выход из диалога, кроме нажатия «Выйти», означает «нет», и требовать за это отдельного нажатия не за что |
| F65 | Кнопки диалога названы действиями, а не «да» и «нет» | 2026-09-07 | «Выйти» и «Остаться» читаются без возврата к вопросу. Подтверждающая кнопка названа тем же словом, что и та, которую нажали, — это то же самое действие, а не новое |
| F66 | В мок добавлены три состояния, которых нет в каталоге | 2026-09-07 | Кабинет обязан показывать архив в обработке, архив со сломанной сборкой и снятый с публикации. На прежних фикстурах эти ветки карточки были бы мёртвым кодом, а `failure_reason` — полем, которого никто ни разу не видел |
| F67 | Ожидание в тестах поднято до 5 с (`asyncUtilTimeout`) | 2026-09-07 | Секунды по умолчанию не хватало на полном прогоне: тест падал под нагрузкой от параллельных файлов и проходил в одиночку. Замечено ещё на S5. Падение «не дождался» неотличимо от настоящего, и такой тест хуже отсутствующего |

| F68 | Технический `draft` назван «не собран», а не «черновик» | 2026-09-07 | Найдено на живом экране. У нового архива оба статуса равны `draft`, и два одинаковых слова подряд читались ошибкой вёрстки. Значения на проводе те же — расходятся подписи, и это правильно: технический статус говорит о файле, а не о витрине |
| F69 | Мок доводит сборку до конца по сроку | 2026-09-07 | Найдено на живом экране. Архив висел в `processing` вечно, опрос из F62 шёл каждые пять секунд и не прекращался никогда — то есть проверить, что он вообще прекращается, было нечем. Срок ставится от загрузки страницы и истекает через полминуты: живьём переход виден, а тесты стартуют со свежей базы и застать его не успевают |
| F70 | Байты выводятся длинной формой единицы, kB и MB — короткой | 2026-09-07 | Найдено на живом экране: у архива без файлов стояло «0 byte». Это не опечатка, а короткая форма CLDR — она для байта совпадает со словом и не склоняется. Длинная даёт «0 bytes» и «0 байт»; у kB и MB сокращение настоящее, им длинная не нужна |
| F71 | Промежутки в шапке поджаты на узком экране | 2026-09-07 | Найдено на живом экране. Пока «Sign out» переносился на две строки, шапка помещалась; `whitespace-nowrap` выпрямил её — и она вылезла за правый край на 5px. Ширину кромки трогать нельзя, она общая (F21), поэтому сжат промежуток между органами управления |

> Решения, затрагивающие общий контракт, сюда не пишем — они идут в `docs/DECISIONS.md`
> после согласования с командой (`docs/INTEGRATION.md` §14).

---

## 7. Открытые вопросы к команде

Статус `open` — ждём ответа; отвечает разработчик соответствующей ветки.

| # | Вопрос | Кому | Статус |
|---|--------|------|--------|
| Q1 | Формат сессии после `/v1/auth/wallet/verify` — httpOnly cookie или Bearer-токен? В `API.md` не указано. Для SPA это принципиально | backend | open |
| Q2 | Транспорт загрузки файлов: signed URL в object storage или proxy через API? | backend | open |
| Q3 | Как загружается cover архива — отдельный эндпоинт или тот же upload flow? | backend | open |
| Q4 | Пагинация каталога: `page` или `cursor`? Форма ответа (`items` + `next`?) | backend | open |
| Q5 | Есть ли публичный агрегат метрик всего маркетплейса для Landing (роль §3.1)? | backend | open |
| Q6 | Откуда фронт берёт ссылку на дистрибутив Viewer (`Download SolArch Viewer`)? | viewer/backend | open |
| Q7 | CORS и домены: фронт и API на разных origin? Влияет на cookie-сессию (SameSite) | backend | open |
| Q8 | Нет эндпоинта списка архивов автора, хотя раздел My Archives обязателен (роль §6.1). Предполагаем `GET /v1/archives` | backend | open |
| Q9 | Правило округления комиссии: `PAYMENTS.md` §5 требует «детерминированное и покрытое тестами», но направление не задано. Мы отсекаем комиссию вниз, автору — остаток. Нужно совпадение с backend до копейки | backend | open |
| Q10 | Считается ли `GET /marketplace/archives/:slug` за `view`? Прелоад по наведению на карточку шлёт этот запрос до перехода — если backend засчитывает view на каждый GET, метрику накручивает наведение мышью, а не просмотр | backend | open |
| Q12 | Чем `GET /v1/me` отвечает гостю — 401 или 200 с пустой сессией? Шапка спрашивает `/me` на каждой публичной странице, и при 401 браузер печатает ошибку в консоль на ровном месте | backend | open |
| Q13 | Рассматривается ли Sign In With Solana (`solana:signIn`) вместо пары challenge/verify? Кошелёк собирает и показывает сообщение сам, по формату SIWS. Это правка общего контракта, поэтому решаем вместе, а не молча | backend | open |
| Q14 | Нет агрегата выручки автора. «Сколько принёс архив за всё время» доступно только через `GET /archives/:id/analytics?period=all`, то есть по запросу на каждый архив. Нужен ли агрегат в списке кабинета — и по автору целиком? | backend | open |
| Q11 | По каким полям работает `search` в каталоге? В `API.md` параметр есть, а состав полей не задан. Мы исходим из того, что ищется и по имени автора: посетитель часто помнит автора, а не название | backend | open |

Как эти вопросы закрыты в коде до ответов: каждое допущение помечено комментарием
`ДОПУЩЕНИЕ (открытый вопрос QN)` и вынесено в одно место, чтобы правка была точечной.

```text
Q1  lib/api/config.ts     API_CREDENTIALS = 'include' (cookie-сессия)
Q2  lib/api/uploads.ts    поддержаны оба транспорта: signed URL и через API
Q4  lib/api/types.ts      paginatedSchema: items/page/per_page/total/has_more
Q8  lib/api/archives.ts   listMyArchives → GET /v1/archives
Q9  lib/money.ts          комиссия floor, автору остаток
Q10 routes/archives.$slug.tsx  loader греет кеш; отключается удалением одной строки
Q11 mocks/handlers/marketplace.ts  поиск идёт по title, short_description и автору
Q12 mocks/handlers/auth.ts    гостю отвечаем 401, клиент читает это как «гость», не как сбой
Q14 routes/dashboard.index.tsx  выручки в списке нет; показана доля автора из economics
```

---

## 8. Журнал сессий

### Сессия 1 — 2026-09-07
- Изучена актуальная документация (`docs/*.md`, `docs/roles/*`).
- Зафиксирована зона ответственности и дорожная карта S0–S13.
- Зафиксирован стек: React+Vite / TanStack Query / Tailwind / shadcn на Base UI (F1–F5).
- `docs/context/*` помечен как устаревший (F6) — больше не читаем.
- Создан `CLAUDE.md` — автозагружаемый сжатый контекст проекта.
- Выбраны роутер, стратегия моков и структура репозитория (F7–F9).
- Найден remote `github.com/kiratonine/SolArch` — пустой, `docs/` туда ещё не залиты.

### Сессия 2 — 2026-09-07 · S0 выполнен

Сделано:
- `git init`, ветка `main` с общей базой (`docs/`, корневой npm workspace, `.gitignore`, README),
  от неё отведена `feat/marketplace-frontend`.
- Создан `apps/web`: Vite 8, React 19, TypeScript 6.
- TanStack Router с файловыми роутами (`src/routes/`), TanStack Query с общим клиентом.
- Tailwind v4 + shadcn/ui на Base UI, пресет `nova` (Lucide + Geist).
- MSW: `handlers.ts` общий, `browser.ts` для dev, `node.ts` для тестов; service worker в `public/`.
- Vitest + Testing Library, незамоканный запрос в тестах падает намеренно.
- oxlint настроен, предупреждений нет.
- Проверено: build, test, lint зелёные; страница рендерится без ошибок в консоли.

Отклонения от плана и почему:
- **oxlint вместо ESLint** — `create-vite@9` перешёл на него по умолчанию; переписывать
  на ESLint не стали, ради скорости и меньшей конфигурации.
- **Убран `baseUrl` из tsconfig** — TypeScript 6 объявил его устаревшим (`TS5101`);
  алиас `@/*` работает через `paths` без него.
- **Пресет темы `nova`** взят как дефолтный. Меняется заменой токенов в `src/index.css`.

Не сделано:
- В GitHub ничего не запушено — ждём согласования с командой по общей базе `main`.

**Следующий шаг:** S1 — typed API client в `lib/api/` и наполнение MSW-хендлеров
по `docs/API.md`. От ответов на Q1–Q7 этап не блокируется: спорные места закрываем
адаптерами, но Q1 (формат сессии) стоит выяснить до S5.

### Сессия 2 (продолжение) — S1 выполнен

Запушено в GitHub: `main` и `feat/marketplace-frontend`.

Сделано:
- `lib/money.ts` — расчёты USDC на `bigint`, превью split 95/5, валидация цены.
- `lib/api/` — `config`, `errors`, `http`, `types`, `query-keys`, `auth`, `marketplace`,
  `archives`, `uploads`, `analytics`, `index`. Все запросы идут через одну функцию `apiRequest`.
- zod-схемы описывают все ответы; типы выводятся через `z.infer`, дублирования нет.
  Несоответствие ответа контракту поднимает `ContractError` прямо на границе сети.
- Загрузка файлов на `XMLHttpRequest` — только он даёт прогресс отправки (роль §9).
- MSW: 5 модулей хендлеров + база в памяти с 6 архивами-фикстурами.
  Реально работают сортировки, скрытие черновиков, логин, create, publish/unpublish,
  переход `processing → ready`, запрет смены цены.
- Тесты: 35 штук. Покрыты деньги, публичный каталог, приватность полей,
  сортировки, auth, неизменяемость цены, публикация, аналитика, проверка контракта.

Проверено: `tsc -b`, `oxlint`, `vitest` (35/35), `vite build` — всё зелёное.
Каталог отрисован в браузере из мока, в консоли ноль ошибок.

Новые открытые вопросы: Q8 (нет эндпоинта списка архивов автора),
Q9 (направление округления комиссии).

**Следующий шаг:** S2 — дизайн-язык: тема, сетка, шапка/подвал, базовые компоненты
(карточка архива, блок метрик, price breakdown, состояния loading/empty/error).

### Сессия 3 — 2026-09-07 · S2 выполнен

По ходу этапа заказчик добавил требование: интерфейс должен быть на двух языках.
Мультиязычность вошла в S2, потому что иначе вся копия S3–S13 писалась бы дважды.

Дизайн-язык — «запечатано на виду»:
- Холодная синеватая бумага и чернильный тёмно-синий вместо белого и чёрного.
- Один акцент — латунный янтарь — закреплён за деньгами: цена, доля автора, «Sol»
  в логотипе. Два токена: `--seal` (заливки) и `--seal-ink` (текст, контраст AA).
- Статусы живут в отдельной приглушённой шкале `--state-*` и с акцентом не конкурируют.
- Шрифты: Unbounded (только h1 и логотип), Golos Text (интерфейс), IBM Plex Mono
  (только машинные строки). Archivo пришлось отклонить — в нём нет кириллицы.
- Теней нет ни одной; глубина — разница бумага/поверхность плюс волосяные линии.
- Главный носитель языка — корешок карточки: левая полоса, которая цветом несёт
  статус архива, и «манифест» внизу: файлы, вес, открытия.

Сделано:
- `src/index.css` — полностью переписанные токены, светлая и тёмная тема.
- `lib/i18n/` — `locale`, `plural`, `format`, `dict.en`, `dict.ru`, `context`,
  `provider`, `use-i18n`. `ru` типизирован как `typeof en`.
- `lib/theme.ts` + `ThemeSwitch` + скрипт предзагрузки темы в `index.html`.
- `components/brand/` — `SealMark`, `Wordmark`.
- `components/layout/` — `Container`, `SiteHeader`, `SiteFooter`, `PageHeader`,
  `LanguageSwitch`, `ThemeSwitch`.
- `components/archive/` — `ArchiveCard`, `StatusBadge`, `MetricGrid`, `PriceTag`,
  `PriceBreakdown`, `tone.ts`.
- `components/state/` — `EmptyState`, `ErrorState`, `ArchiveCardSkeleton`.
- Добавлены примитивы shadcn: `badge`, `skeleton`.
- Каталог на главной перерисован на новом языке, со всеми тремя состояниями.
- Тесты: 61 (было 35). Покрыты словари, плюрализация, форматы, выбор локали и темы,
  карточка, статусы, разбивка цены, состояния, переключение языка.

Отклонения от плана и почему:
- **Мультиязычность не была в плане S2** — требование пришло в середине этапа.
- **Archivo → Unbounded + Golos Text** — Archivo не покрывает кириллицу.
- **Ширина 54rem вместо 70rem** — при 70rem шапка уезжала шире содержимого;
  общая кромка важнее запаса ширины, которым пока никто не пользуется.
- **Добавлен переключатель темы** — тёмная палитра без него была бы мёртвым кодом.

Не сделано осознанно:
- `ArchiveCard` пока презентационная, без ссылки и без наведения: роут
  `/archives/$slug` появится на S4, а выдумывать заглушку-страницу не стали.
- В шапке нет `Sign in` и `Get the Viewer` — за ними нет ни роутов (S5),
  ни ответа на Q6. Мёртвых ссылок в шапке нет.

**Следующий шаг:** S3 — каталог: сортировки в search params, поиск, ссылка на
страницу архива и наведение на корешок карточки. Компоненты и состояния готовы.

### Сессия 4 — 2026-09-07 · S3 выполнен

Каталог перестал быть витриной с одним состоянием: сортировка, поиск и страница
переехали в адресную строку, поэтому любую выдачу можно переслать ссылкой,
положить в закладки и вернуть кнопкой «назад».

Сделано:
- `lib/catalog-search.ts` — разбор и сборка параметров каталога в одном месте
  (F22–F24). Мусор в адресе гасится в умолчание, умолчания в адресе не хранятся.
- `components/catalog/` — `SortNav` (пять сортировок ссылками), `SearchField`
  (пауза 300 мс, `replace`, поле догоняет адрес), `CatalogPagination`, `HowItWorks`.
- `routes/index.tsx` — `validateSearch`, `keepPreviousData`, отдельное пустое
  состояние для поиска («по запросу … ничего нет») с ссылкой «очистить».
- `routes/archives.$slug.tsx` — роут публичной страницы архива: название, автор,
  цена, описание, метрики, кнопка «Download .slr» и состояние «архив недоступен».
- `ArchiveCard` стала ссылкой: `after:inset-0` растягивает клик на карточку,
  имя ссылки остаётся коротким — только название. Корешок утолщается при наведении.
- `lib/query-client.ts` — 4xx больше не повторяются (F29).
- Мок: `PER_PAGE` 12 → 6, добавлены четыре архива (девять опубликованных, две страницы).
- `test/render.tsx` — два новых стенда: `renderWithRouter` (компонент со ссылкой)
  и `renderApp` (всё приложение по настоящему дереву маршрутов через MSW).
- Тесты: 90 (было 61). Разбор параметров, сортировка из адреса и кликом, поиск
  из адреса и с клавиатуры, пустая выдача поиска, постраничность, переход на
  страницу архива, её содержимое, отсутствующий архив, обе локали.

Отклонения от плана и почему:
- **Роут `/archives/$slug` сделан на S3, хотя числился за S4.** S3 обязан был дать
  ссылку с карточки, а ссылка в никуда — это не деливери. Страница сделана в объёме
  «зачем гость пришёл»: описание, цена, метрики, кнопка скачивания. За S4 остаются
  публичный список файлов, условия лицензии (max_devices, export, watermark),
  обложка и ссылка на Viewer.
- **Поиск и сортировка встали разными строками.** В одной строке пять русских
  подписей плюс поле поиска не помещались в меру 54rem и разваливались переносом —
  видно только в браузере, тесты этого не ловят.
- **`PER_PAGE` в моке уменьшен** — иначе постраничность нечем было проверить (F30).

Найдено на живой странице, а не тестом:
- Ввод в поиск утаскивал страницу к шапке: `scrollRestoration` роутера отрабатывал
  на каждой записи в адрес. Лечится `resetScroll: false` (F27). Тем же флагом
  закрыты чипы сортировки — список стоит прямо под ними.

Известный долг (не S3, но записываем, чтобы не потерялся):
- `toUserMessage()` в `lib/api/errors.ts` возвращает три строки на русском мимо
  словаря: «Сервер недоступен», «Сервер вернул неожиданный ответ», «Неизвестная
  ошибка». В английской локали они прорвутся как есть. Чинить на S11 вместе
  с остальным разбором ошибок: функция чистая и словаря не видит, поэтому ей
  нужен параметр, а не контекст.

Не сделано осознанно:
- В шапке по-прежнему нет `Sign in` и `Get the Viewer` — за ними нет ни роутов (S5),
  ни ответа на Q6. Мёртвых ссылок не заводим.
- Агрегата метрик всего маркетплейса на Landing нет: Q5 открыт, а выдумывать
  число, которое потом разойдётся с backend, нельзя.

Отложено осознанно (решено в конце сессии 4):
- Прелоад **данных** страницы архива по наведению на карточку. Сейчас роутер
  преподгружает только код страницы (`defaultPreload: 'intent'`), а запрос стартует
  после клика — отсюда секунда скелета. Лечится `loader` на роуте через
  `queryClient.ensureQueryData`. Делать на S4, когда страница и так дорабатывается.

**Следующий шаг:** S4 — публичная страница архива целиком: список файлов
(`display_path`, `display_name`, `extension`, `mime_type`, `size_bytes` и ничего
сверх), условия лицензии, обложка и объяснение, где происходит оплата.
Плюс отложенный прелоад данных по наведению.

### Сессия 5 — 2026-09-07 · S4 выполнен

Страница архива перестала быть карточкой с кнопкой. Гость платит уже после того,
как файл у него, поэтому всё, ради чего он сюда пришёл, обязано быть сказано до
скачивания: что внутри, что даст оплата и где именно её возьмут.

Сделано:
- `lib/files.ts` — группировка публичного списка по `display_path` (F31). Корень
  всплывает наверх, вложенные папки не разбираются на уровни, порядок backend цел.
- `components/archive/file-manifest.tsx` — опись: заголовок папки (моно, путь плюс
  состав), под ним файлы волосяными строками, веса табличными цифрами справа.
- `components/archive/license-terms.tsx` — `license_policy` прозой: «одно устройство»,
  «без экспорта», «с водяным знаком». Разрешённый экспорт даёт другую строку,
  выключенный водяной знак — ни одной (F33).
- `components/archive/archive-cover.tsx` — обложка в жёстком кадре, битая ссылка
  убирает блок целиком (F34).
- `routes/archives.$slug.tsx` — `loader` греет кеш через `prefetchQuery` (F35),
  список файлов живёт отдельным запросом со своей судьбой, объяснение оплаты
  собрано в один абзац с ценой из ответа backend.
- Мок: три обложки как data URI, у первого архива появились корень и вложенная
  папка — иначе опись проверялась бы на одном плоском случае.
- Тесты: 107 (было 90). Группировка, опись и её приватность, сбой списка файлов
  без падения страницы, три условия лицензии и вариант без водяного знака,
  наличие и отсутствие обложки, обе локали, разные авторы в каталоге, поиск
  по автору, кабинет автора без чужих архивов.

Правки по ходу сессии — заказчик смотрел живую страницу:
- Чипы сортировки вылезали за левую кромку: активный чип нарисован рамкой, а строка
  сдвигалась на `-ml-2.5` «под текст». Сдвиг убран, грань чипа совпала с полем поиска.
- «Вернуться в каталог» была подписью, а не органом управления. Стала чипом с рамкой
  и стрелкой — той же формы, что и чипы сортировки.
- Знак логотипа читался кривым: угол срезался у самого силуэта. Силуэт стал целым,
  печать ушла внутрь контура (F37), а сам логотип собран в один локап `Logo`
  с оптической поправкой подъёма (F36).
- Из шапки убран слоган «Запечатанные файлы, открытый каталог» вместе с ключом
  `brand.tagline` в обеих локалях — мёртвых ключей не держим.

Найдено на живой странице, а не тестом:
- В описи файлы под папкой не были отбиты отступом. Тест видел правильное дерево
  разметки и проходил, а глаз читал плоский список: заголовок `security/` стоял
  в один ряд со своим же содержимым. Лечится отступом `pl-9` у строк внутри папки.
- Подписи метрик оказались в разном регистре: `вес` строчной, `Просмотры` с прописной.
  Пока подписи стояли по одной, это не читалось; на странице архива их пять в ряд.
  Все приведены к строчным — под числом подпись это хвост числа, а не заголовок.
  Заодно удалён ключ `metrics.files`: карточки берут `units.files`, а он не звался нигде.
- Все девять архивов были подписаны одним автором «Aurora Labs»: `toListItem` подставлял
  имя из `MOCK_CREATOR`, а не из архива. Теперь у каждой фикстуры свой автор, а у мока
  появился владелец архива (F38) — заодно `GET /archives` перестал отдавать в кабинет
  весь каталог, что иначе всплыло бы на S6 как «мой кабинет полон чужого».
- Поиск не находил по имени автора (F39). Плейсхолдер поля переписан: «Название, автор
  или описание». Состав полей поиска — допущение, вынесено в Q11.
- Счётчик «9 архивов» в шапке каталога убран (F40) вместе с пропом `aside` у `PageHeader`
  и ключом `units.archives` — оба остались бы мёртвыми.
- Блок трёх шагов с главной убран целиком (F41): `components/catalog/how-it-works.tsx`
  и ключ `catalog.steps` в обеих локалях удалены. Тест «объясняет механику продукта»
  переписан на подзаголовок и подвал — они и остались носителями объяснения.

Не сделано осознанно:
- Ссылки на дистрибутив Viewer на странице по-прежнему нет: Q6 открыт. Страница
  объясняет словами, что архив открывает Viewer и оплата идёт там, но ведёт в никуда.
  Это первый кандидат на закрытие, как только придёт ответ.
- Обложка в карточках каталога не появилась: карточка — текстовая опись, и вводить
  в неё картинку значит переписывать язык каталога. Обложка живёт на странице архива.

Новый открытый вопрос: Q10 — считается ли прелоад по наведению за просмотр.

**Следующий шаг:** S5 — creator auth: wallet challenge/verify, guard роутов, сессия.
До начала этапа желателен ответ на Q1 (cookie или Bearer): от него зависит,
хранит ли фронт токен вообще.

### Сессия 6 — 2026-09-07 · S5 выполнен

Вход автора. Главный вопрос этапа был не «как сверстать форму», а «откуда браузер
берёт подпись»: пароля у автора нет, есть кошелёк.

Решение по слою кошелька (F42, F43): берём реестр Wallet Standard готовым, а UI и
связку challenge/verify пишем свои. Сниффинг `window.phantom` — это переписанный
от руки протокол, который стандарт и заменил; `@solana/wallet-adapter` — RPC и
модалка, которые нам не нужны ни на день. Итог — 4 маленьких пакета (два из них
только типы) и ~90 строк своего кода, из которых половина комментарии.

Сделано:
- `lib/wallet/` — граница, за которой Wallet Standard не виден: `detectWallets`,
  `onWalletsChange`, `connect → signMessage` с подписью в base64 и `WalletError`,
  который отличает отказ человека от поломки расширения.
- `lib/wallet/use-wallets.ts` — `useSyncExternalStore` над реестром: расширения
  регистрируются асинхронно и появляются уже после первой отрисовки.
- `routes/login.tsx` — вход в два шага (F44) с состояниями: подключаем, подписываем,
  проверяем, отказ, кошелька нет, сервер не ответил.
- `routes/dashboard.tsx` — guard в `beforeLoad` (F45) с адресом возврата;
  `dashboard.index.tsx` — честный каркас без выдуманного пустого состояния (F51).
- `lib/auth/redirect.ts` — `safeRedirect` против открытого редиректа (F46).
- `query-client.ts` — `createQueryClient()` и один обработчик 401 на весь клиент (F47).
- Шапка: адрес кошелька и выход для вошедшего, «Войти» для гостя.
- `mocks/demo-wallet.ts` — кошелёк для разработки через тот же реестр (F48).
- Тесты: 129 (было 107). Guard и адрес возврата, подмена `redirect` на чужой сайт,
  полный путь до кабинета, отказ подписать, отсутствие кошелька, выход, вошедший
  на `/login`, истёкшая сессия, обе локали, фильтр кошельков без `signMessage`.

Найдено на живой странице, а не тестом:
- В плите с сообщением адрес кошелька уезжал за правый край на 390px: `whitespace-pre-wrap`
  не переносит строку из 44 знаков без пробелов. Плита существует ровно затем, чтобы
  сообщение читалось целиком, — поставлен `wrap-anywhere`.
- Шапка вошедшего автора не помещалась в 390px и давала горизонтальную прокрутку
  всей страницы. Ссылка «Каталог» скрыта на узком экране (F50).
- Заголовки разделов на новой странице набрались Unbounded: базовый слой назначает
  `font-display` всем h1–h3, а правило разрешает его только h1. Локальный помощник
  со страницы архива вынесен в общий `SectionHeading` (F49).
- «Выбрать другой кошелёк» кнопкой-призраком читалось подписью, а не действием —
  та же болезнь, что чинили на S4 у «Вернуться в каталог». Дана рамка.
- В каркасе кабинета стояло «Вы ещё не создали ни одного архива» — неправда:
  список ещё не запрашивается, а в моке архивы есть (F51).

Не сделано осознанно:
- Связка «401 → `router.invalidate()`» проверена по частям: обработчик клиента —
  юнит-тестом, выход из кабинета по обнулённой сессии — сквозным. Целиком она
  сойдётся на S6, когда в кабинете появятся собственные запросы.
- Ссылки на дистрибутив Viewer по-прежнему нет: Q6 открыт.

Новые открытые вопросы: Q12 (чем `/me` отвечает гостю) и Q13 (SIWS вместо challenge/verify).

Правки по ходу сессии — заказчик смотрел живой экран:
- Ссылки шапки стали основного цвета, активная получила подчёркивание (F52).
- Адрес кошелька убран в плашку к переключателям языка и темы (F53).
- Раздел описи переименован в «Содержимое», пояснение под заголовком убрано (F54).
- Подвал: два абзаца прозы сначала переложены в колофон «термин — строка», а затем
  убраны целиком по решению заказчика (F55). Остались логотип и год. Продуктовые
  объяснения собираем на отдельной странице «как это работает».

Замечено на будущее: тест каталога «показывает опубликованные архивы» один раз упал
на `findAllByRole('article')` при полном прогоне и прошёл при повторе — стандартный
таймаут в 1 с под нагрузкой. Если повторится, ждать выдачу явным `waitFor`.

**Следующий шаг:** S6 — Creator Dashboard: список своих архивов, статусы, цена, метрики.
Данные уже есть: `GET /v1/archives` в моке отдаёт архивы владельца (F38).

### Сессия 7 — 2026-09-07 · S6 выполнен

Кабинет автора получил содержимое. Вёрстки здесь было меньше, чем решений о том,
что именно кабинет вправе утверждать.

Главный вопрос этапа — «revenue» из строки дорожной карты. Числа «заработано»
в контракте нет: есть цена, есть доля автора в `economics` и есть счётчик открытий.
Перемножить их — значит выпустить на экран число, за которое никто не отвечает
(возвраты, частичные периоды, разные комиссии у разных архивов). Поэтому в списке
стоит то, что прислал backend: цена и доля автора с каждого открытия (F59).
Выручка за период придёт из `analytics` на S10, а нужен ли кабинету общий агрегат —
это Q14 к backend.

Сделано:
- `components/archive/creator-archive-card.tsx` — карточка кабинета: оба статуса
  порознь (F57), доля автора рядом с ценой, причина неудавшейся сборки, манифест
  из пяти колонок и ссылка на публичную страницу у опубликованного.
- `components/archive/tone.ts` — `creatorSpineTone`: правило, как один корешок
  показывает два статуса (F58). Живёт рядом со шкалой, а не в компоненте.
- `routes/dashboard.index.tsx` — запрос, порядок «новые сверху» (F61), скелет,
  ошибка с повтором, пустое состояние. Прелоад списка в `loader`.
- `lib/api/archives.ts` — `myArchivesQuery` опрашивает backend, пока хоть один
  архив в `uploading` или `processing` (F62).
- `mocks/db.ts` — три фикстуры под состояния, которых в каталоге не бывает:
  в обработке, со сломанной сборкой и снятая с публикации (F65). Поле
  `failure_reason` в моке появилось вместе с ними.
- `components/ui/dialog.tsx` — модальный диалог на примитивах Base UI, без тени,
  как и всё остальное в теме. Закрывается кнопкой, Escape и кликом мимо.
- `components/auth/sign-out-button.tsx` — выход с подтверждением (F63–F65);
  логика выхода уехала из шапки в отдельный компонент.
- Тесты: 144 (было 129). Свои архивы против чужих, порядок списка, оба статуса
  на трёх разных архивах, цена и доля автора, причина сбоя, наличие и отсутствие
  публичной ссылки, отсутствие мок-полей в выдаче, пустой кабинет, сбой и повтор,
  русская локаль; подтверждение выхода, отказ, клик мимо и Escape; неповторяющиеся
  подписи у нового архива и законченная сборка.

Правки по ходу сессии — просьбы заказчика:
- Выход по одному нажатию заменён на подтверждение (F63). Кнопка стоит вплотную
  к ссылкам шапки, и промах по ней выбрасывал из кабинета мгновенно.
- Диалог сперва был `AlertDialog` и по замыслу не закрывался кликом мимо. Заменён
  на обычный `Dialog` (F64): вопрос «выйти?» не то, ради чего стоит запирать экран.

Починено по дороге:
- Тест каталога, который на S5 один раз упал под нагрузкой и прошёл при повторе,
  упал снова — теперь на кабинете. Причина та же: секунда ожидания по умолчанию.
  Ожидание поднято до пяти секунд один раз на весь прогон (F66).

Не сделано осознанно:
- Карточка кабинета никуда не ведёт: страницы `/dashboard/:id` ещё нет, а ссылка
  в никуда хуже её отсутствия. Единственная ссылка ведёт туда, где архив уже
  существует для постороннего, — на публичную страницу.
- В пустом состоянии нет кнопки «создать архив»: формы создания нет до S7.
- Мок не доводит фикстуру из `processing` в `ready` сам. Настоящий переход даст
  загрузка на S8; выдумывать таймер в моке до неё незачем.

Найдено на живом экране, а не тестом (браузер освободился к концу сессии):
- У нового архива стояло «Черновик» дважды подряд: технический и маркетплейсный
  `draft` переводились одним словом. Технический назван «Не собран» (F68).
- Архив в `processing` не выходил из него никогда, и опрос из F62 шёл вечно.
  Мок теперь доводит сборку по сроку (F69) — переход виден на экране, а опрос
  после него прекращается; проверено: за 14 секунд ни одного запроса.
- У архива без файлов стоял «0 byte». Байты переведены на длинную форму
  единицы (F70): «0 bytes» и «0 байт».
- На 390px шапка вылезала за правый край на 5px. Сперва это пряталось за тем,
  что «Sign out» переносился на две строки; `whitespace-nowrap` выпрямил строку
  и обнажил переполнение. Промежутки в шапке поджаты (F71).

Замечено, оставлено как есть: на 390px пять колонок манифеста переносятся 4 + 1,
и «unlocks» оказывается один на второй строке. Пять колонок в 340px не помещаются
никак, а прятать метрику у узкого экрана — значит показывать разное разным людям.

**Следующий шаг:** S7 — Create Archive: форма, экономика 95/5, предупреждение
о неизменности цены, payout wallet с проверкой Solana-адреса.
