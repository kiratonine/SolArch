import { cn } from '@/lib/utils'

/**
 * Знак SolArch: лист с запечатанным углом.
 *
 * Силуэт — целый прямоугольник с одинаковыми полями со всех сторон: контур
 * контейнера виден полностью, и это буквально «запечатано на виду». Раньше угол
 * срезался у самого силуэта, и на 16–18 пикселях знак читался не как печать,
 * а как криво обрезанный прямоугольник.
 *
 * Печать — янтарный угол внутри контура, ровно 45°: тот же жест, что и корешок
 * карточки архива, и тот же единственный акцент, что у цены.
 */
export function SealMark({ className }: { className?: string }) {
  return (
    <svg
      viewBox="0 0 16 16"
      aria-hidden="true"
      focusable="false"
      className={cn('size-4 shrink-0', className)}
    >
      <path
        d="M4 1.5H12a1.25 1.25 0 0 1 1.25 1.25V13.25a1.25 1.25 0 0 1-1.25 1.25H4a1.25 1.25 0 0 1-1.25-1.25V2.75A1.25 1.25 0 0 1 4 1.5Z"
        fill="currentColor"
      />
      {/* Печать занимает 4.5 из 10.5 единиц ширины: меньше — сливается с корпусом
          и знак становится сплошным пятном, больше — перевешивает силуэт. */}
      <path d="M8.75 1.5H12a1.25 1.25 0 0 1 1.25 1.25V6Z" fill="var(--seal)" />
    </svg>
  )
}
