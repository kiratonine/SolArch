# Базовый URL Marketplace Backend (см. docs/API.md, префикс /v1)
VITE_API_BASE_URL=http://localhost:3000

# Мок backend через MSW, только в dev-сборке. false — работать против настоящего
# backend по адресу выше; войти тогда можно только настоящим кошельком.
VITE_ENABLE_MSW=true
